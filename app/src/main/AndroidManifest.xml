<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.raikar.samplebranchapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BranchApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SampleBranchApp">

        <!--Default landing screen-->
        <activity android:name=".AppLandingActivity" >
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>


        <!--Branch-IO Link-->
        <activity android:name=".BranchDeepLinkActivity"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Branch URI Scheme -->
            <intent-filter>
                <data
                    android:scheme="raikar"
                    android:host="open"/>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>

            <!-- Branch App Links (optional) -->
            <!--<intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="raikar.app.link"
                    android:scheme="https" />
                <data
                    android:host="raikar.test-app.link"
                    android:scheme="https" />
            </intent-filter>-->

        </activity>

        <meta-data
            android:name="io.branch.sdk.BranchKey"
            android:value="key_live_mm9DjSYcGjhcmdRqOZ4xEdbasxjLgCUB" />
        <meta-data
            android:name="io.branch.sdk.BranchKey.test"
            android:value="key_test_dlXAbJZgPddhcaUq9eauWhgbEEgSoxSM" />
        <meta-data
            android:name="io.branch.sdk.TestMode"
            android:value="true" />

        <activity android:name=".MyOrdersActivity"/>
        <activity android:name=".MyWishListActivity"/>
    </application>

</manifest>